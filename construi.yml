image: lstephen/docker:latest
privileged: true

environment:
  - DOCKERHUB_USERNAME
  - DOCKERHUB_PASSWORD
  - GIT_AUTHOR_NAME
  - GIT_AUTHOR_EMAIL

targets:
  build: docker build 2.4/

  publish: /bin/bash construi/publish/run.sh


